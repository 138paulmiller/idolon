$demo/demo python
from idolon import *

sheet("demo/demo")
s = sprite(0,0,0)
dt = 0

def onkey(k, dwn):
  global s, dt
  if dwn:
    if k == DOWN:
      dt-=1
    if k == UP:
      dt+=1
    flip(s, dt)

def update():
  global s
  pos(s, mx(), my())
  print("update:", pos(s))

